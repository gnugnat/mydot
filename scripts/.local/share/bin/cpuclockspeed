#!/usr/bin/env bash


# Copyright (c) 2020, XGQT
# Licensed under the ISC License


trap 'exit 128' INT
export PATH


speeds=$(grep -i mhz /proc/cpuinfo | cut -d ":" -f 2)
CPU=0

for speed in ${speeds}
do
    ((CPU++))
    echo "CPU ${CPU}: ${speed}"
done
